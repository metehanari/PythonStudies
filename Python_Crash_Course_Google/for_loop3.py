product = 1 #initialize value

for n in range(1,10):  # we use range(1,x) instead of range(x) cause of starting value.
    product = product * n   # product  multiple by n(1..10) and assign to product

print(product)