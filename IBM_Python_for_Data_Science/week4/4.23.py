#numpy linspace

import numpy as np

test_interval = np.linspace(-2, 2, num=5)
# create a numpy array with 5 elements from -2 to 2.
# start from -2, end at 2, and have 5 elements

print(test_interval)